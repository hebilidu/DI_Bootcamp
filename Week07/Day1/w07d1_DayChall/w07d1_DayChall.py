import random
import numpy as np

while True:
    input_n = input("Enter N (1<N<40): ")
    input_m = input("Enter M (1<M<50): ")
    n = int(input_n)
    m = int(input_m)

randrown = np.random.randint(0,100,n)
randrowm = np.random.randint(0,100,m)

# Third row

# Third column

# Set all last row elements to 7

# Set last column elements to sum of first 2 columns 